[package]
name = "lanedeck-shell"
edition = "2024"
license = "MIT"
repository = "https://github.com/atticusdeng/LaneDeck"
rust-version = "1.93"
version = "0.0.0"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]
name = "lanedeck_shell"

[dependencies]
tauri = { version = "2", features = [] }
