{
  "name": "@lanedeck/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --check . --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "@lanedeck/protocol": "workspace:*",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
